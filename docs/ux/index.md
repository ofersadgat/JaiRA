# UX

Standing doc: [principles.md](principles.md). Template: [_templates/ux-pattern.md](../_templates/ux-pattern.md).

## Patterns

One interaction shape per doc. A feature reuses a pattern that does its job unchanged and adds a pattern when none does.

| Pattern | Use when | Serves | Status |
| --- | --- | --- | --- |
| _none yet_ | | | |

## State vocabulary

Patterns, components and failure modes use the same state names: `first_run`, `empty`, `loading`, `partial`, `error`, `denied`, `success`.
