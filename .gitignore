node_modules/
dist/
*.tsbuildinfo

# Secrets. Provider keys live in the environment (see README) — never in a commit.
.env
.env.*
!.env.example

# Snapshot evidence: pictures of the specimens and the reference figures, regenerated by
# `npm --workspace @jaira/app run snapshot`. Evidence for a comparison, not an artifact of the
# build — a committed PNG goes stale the first time a hairline moves.
packages/app/snapshot/out/
